/* Variables and function declarations are moved to the top of their scope before code execution; only works for var and function declarations, not with let or const. This means that no matter where functions and variables are declared, they are moved to the top of their scope regardless of whether their scope is global or local
*/
